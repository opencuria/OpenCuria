# Docker SDK
docker>=7.0,<8.0

# QEMU/KVM runtime deps live in requirements-qemu.txt (optional for docker-only dev).

# WebSocket Client (async)
python-socketio[asyncio_client]>=5.0,<6.0
aiohttp>=3.13.3

# Configuration
pydantic-settings>=2.0,<3.0

# Logging
structlog>=24.0,<25.0

# CLI
typer>=0.9,<1.0
rich>=13.0,<14.0

# System metrics
psutil>=5.9,<7.0

# Testing
pytest>=9.0.3,<10.0
pytest-asyncio>=1.3.0,<2.0
