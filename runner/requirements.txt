# Docker SDK
docker>=7.0,<8.0

# QEMU/KVM runtime (optional — only needed when RUNNER_ENABLED_RUNTIMES includes "qemu")
libvirt-python>=10.0.0
asyncssh>=2.14,<3.0

# WebSocket Client (async)
python-socketio[asyncio_client]>=5.0,<6.0
aiohttp>=3.13.3

# Configuration
pydantic-settings>=2.0,<3.0

# Logging
structlog>=24.0,<25.0

# CLI
typer>=0.9,<1.0
rich>=13.0,<14.0

# System metrics
psutil>=5.9,<7.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
